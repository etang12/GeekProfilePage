import React from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const UpdatePicturePage = props => {
  return (
    <View style={styles.mainContainer}>
      <Text>Picture</Text>
    </View>
  );
};

export default UpdatePicturePage;
