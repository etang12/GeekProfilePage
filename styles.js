import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  profileImage: {
    width: 200,
    height: 200,
    // borderRadius: 10,
    // overflow: 'hidden',
    // resizeMode: 'cover',
  },
});

export default styles;
